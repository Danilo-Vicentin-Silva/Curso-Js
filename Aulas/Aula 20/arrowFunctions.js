// Arrow Functions são normalmente ultilizadas para agilizar na hora de digitar functions pequenas
// Elas são mais atuais e avançadas que as functions normais
// Digitar 'return' não é necessário, pois por padrão, tudo que está digitado nelas irá ganhar o return

const functionArrow = (params) => console.log('Essa é uma Arrow Function')

const message = (msg) => console.log(msg);

message('Olá');
